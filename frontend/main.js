// Main.js - Yeni modüler yapı
// Artık sadece eski uyumluluğu korumak için kullanılıyor
// Yeni kod için app.js kullanın

import { AppManager } from './app.js';

// Eski main.js'nin çalışması için AppManager'ı başlat
AppManager.initialize();
